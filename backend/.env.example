DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hris"
JWT_SECRET="replace-with-long-random-string"
PORT=4000
CORS_ORIGIN="http://localhost:5173"

# Optional: real email (leave unset to log to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Email all employees when payroll lines are generated (set to "true")
PAYROLL_NOTIFY_EMAIL=false
